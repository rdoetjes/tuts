\ print to the screeen 
: GREETING   .( Hello World ! ) CR ;
: GREETING2  ." Or like this!" CR ;

\ call the word GREETING (basically calling the procedure)
GREETING
\ call the word GREETING2 (basically calling the procedure)
GREETING2

\ and bugger of back to the OS prompt
BYE

